#/usr/bin/env bash

set -e
set -u
set -o pipefail

NAME="{{ cluster.name }}"
KOPS_STATE_STORE="s3://{{ cluster.s3_bucket_name }}/{{ cluster.name }}"


echo "#------------------------------------------------------------"
echo "# Switch context for kubectl"
echo "#------------------------------------------------------------"
echo "kubectl config use-context ${NAME}"
